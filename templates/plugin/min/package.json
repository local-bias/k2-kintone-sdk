{
  "name": "k2-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "init": "plugin init",
    "dev": "plugin dev",
    "build": "plugin build && plugin zip",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@kintone/dts-gen": "^9.0.8",
    "@konomi-app/k2": "^5.0.0",
    "typescript": "^7.0.2"
  }
}
